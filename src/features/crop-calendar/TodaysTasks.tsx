import { Check, Clock3 } from 'lucide-react';
import { AnimatePresence, motion } from 'framer-motion';
import type { CalendarTask } from './types';

export function TodaysTasks({ tasks, onChange }: { tasks: CalendarTask[]; onChange: (task: CalendarTask) => void }) {
  return <aside aria-labelledby="today-tasks" className="rounded-3xl border border-emerald-100 bg-white/75 p-5 shadow-[0_18px_45px_-30px_rgba(16,100,58,.6)] backdrop-blur-xl dark:border-emerald-900/70 dark:bg-slate-900/75"><div className="mb-4"><p className="text-xs font-bold uppercase tracking-[.16em] text-emerald-600">Stay on track</p><h2 id="today-tasks" className="mt-1 text-xl font-bold text-slate-900 dark:text-white">Today’s tasks</h2></div><ul className="space-y-3">{tasks.map(task => <li key={task.id}><motion.button layout onClick={() => onChange({ ...task, completed: !task.completed })} className={`flex w-full items-center gap-3 rounded-2xl border p-3 text-left transition-colors ${task.completed ? 'border-emerald-100 bg-emerald-50/75 dark:border-emerald-900 dark:bg-emerald-950/40' : 'border-slate-100 bg-white/70 hover:border-emerald-200 dark:border-slate-700 dark:bg-slate-950/30'}`}><motion.span animate={{ scale: task.completed ? 1 : .92 }} className={`grid h-6 w-6 shrink-0 place-items-center rounded-full border ${task.completed ? 'border-emerald-600 bg-emerald-600 text-white' : 'border-slate-300 dark:border-slate-600'}`}><AnimatePresence>{task.completed && <motion.span initial={{ scale: 0 }} animate={{ scale: 1 }}><Check className="h-4 w-4" /></motion.span>}</AnimatePresence></motion.span><span className="min-w-0 flex-1"><strong className={`block text-sm ${task.completed ? 'text-slate-400 line-through' : 'text-slate-800 dark:text-slate-100'}`}>{task.title}</strong><span className="mt-1 flex items-center gap-1 text-xs text-slate-500"><Clock3 className="h-3 w-3" />{task.dueTime}</span></span><span className={`rounded-full px-2 py-1 text-[10px] font-bold uppercase ${task.priority === 'High' ? 'bg-rose-100 text-rose-700 dark:bg-rose-950/50 dark:text-rose-300' : task.priority === 'Medium' ? 'bg-amber-100 text-amber-700 dark:bg-amber-950/50 dark:text-amber-300' : 'bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-300'}`}>{task.priority}</span></motion.button></li>)}</ul></aside>;
}
